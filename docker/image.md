
# Image

- 앱을 실행하기 위해 필요한 모든 요소를 모아 놓은 환경.
- 파일, 라이브러리, 설정, 소스 코드 등이 포함됨.
- 한 번 만들어지면 수정할 수 없음.

# Layer

- `Dockerfile`의 명령어 한 줄 한 줄이 하나의 레이어로 작용.
- 한 번 만들어진 레이어는 변경 시 수정되지 않고 교체됨.
- 아래에서 위로 쌓이는 스택. 파일 이름이 같을 경우 상단이 하단을 가림.
- 빌드 시 레이어가 변화하지 않았다면 생성하지 않고 캐싱함.